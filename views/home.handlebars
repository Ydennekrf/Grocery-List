<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

  {{#if recipes.length}}
  <div class="col-md-6 recipe-list">
    <h3>Current Recipes:</h3>

    {{#each recipes as |recipe|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/recipes/{{recipe.id}}">{{recipe.name}}</a></h4>
      
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="/models/recipes.js"></script>